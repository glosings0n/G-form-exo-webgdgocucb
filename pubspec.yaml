name: g_exo_form_web_gdgocucb
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  firebase_core: ^3.6.0
  flutter:
    sdk: flutter
  gap: ^3.0.1
  hugeicons: ^0.0.7

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
